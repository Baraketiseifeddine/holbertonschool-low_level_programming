#include "holberton.h"
/**
 * _isupper - use only user defined variables
 * @c: Variable to check ascii code
 *
 * Return: 0
**/
int _isupper(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
