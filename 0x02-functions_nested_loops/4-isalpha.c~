#include "holberton.h"
/**
 * _isalpha - use only user defined variables
 * @c: is variable
 * Return: 0
**/
int _isalpha(int c)
{
if (c <= 'z' && c >= 'a')
else if (c <= 'Z' && c >= 'A')
return (1);
else
return (0);
}
